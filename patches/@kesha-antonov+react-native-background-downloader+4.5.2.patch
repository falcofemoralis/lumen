diff --git a/node_modules/@kesha-antonov/react-native-background-downloader/android/local.properties b/node_modules/@kesha-antonov/react-native-background-downloader/android/local.properties
new file mode 100644
index 0000000..b2bc356
--- /dev/null
+++ b/node_modules/@kesha-antonov/react-native-background-downloader/android/local.properties
@@ -0,0 +1,8 @@
+## This file must *NOT* be checked into Version Control Systems,
+# as it contains information specific to your local configuration.
+#
+# Location of the SDK. This is only used by Gradle.
+# For customization when using a Version Control System, please read the
+# header note.
+#Thu Feb 26 15:47:56 EET 2026
+sdk.dir=D\:\\Programs\\Android\\Sdk
diff --git a/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/RNBackgroundDownloaderModuleImpl.kt b/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/RNBackgroundDownloaderModuleImpl.kt
index b00d138..d62a73f 100644
--- a/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/RNBackgroundDownloaderModuleImpl.kt
+++ b/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/RNBackgroundDownloaderModuleImpl.kt
@@ -713,11 +713,11 @@ class RNBackgroundDownloaderModuleImpl(private val reactContext: ReactApplicatio
 
     // On Android 16+ (API 36), DownloadManager has strict path restrictions and throws
     // SecurityException for app-specific external storage paths. Use ResumableDownloader instead.
-    if (Build.VERSION.SDK_INT >= 36) {
+    // if (Build.VERSION.SDK_INT >= 36) {
       logD(NAME, "Android 16+ detected: Using ResumableDownloader to avoid DownloadManager path restrictions")
       startWithResumableDownloader()
       return
-    }
+    // }
 
     if (isValidExternalPath) {
       // Use DownloadManager with valid external storage path
@@ -1041,9 +1041,36 @@ class RNBackgroundDownloaderModuleImpl(private val reactContext: ReactApplicatio
 
       // Phase 4: Add active resumable downloads (in-progress via ResumableDownloader)
       val resumableDownloader = downloader.resumableDownloader
-      // Check for any paused resumable downloads that weren't in the persisted state
-      // (e.g., paused during current session but not yet persisted)
-      // This is handled by the pausedDownloads map above since we persist on pause
+      val activeResumableDownloads = resumableDownloader.getAllActiveDownloads()
+      
+      for ((configId, downloadState) in activeResumableDownloads) {
+        // Skip if already processed in previous phases
+        if (processedIds.contains(configId)) {
+          continue
+        }
+
+        val params = Arguments.createMap()
+        params.putString("id", configId)
+        
+        // Get metadata from the configIdToMetadata map, fallback to empty JSON
+        val metadata = configIdToMetadata[configId] ?: "{}"
+        params.putString("metadata", metadata)
+        
+        // Determine state based on whether the download is paused
+        val state = if (downloadState.isPaused.get()) {
+          DownloadConstants.TASK_SUSPENDED
+        } else {
+          DownloadConstants.TASK_RUNNING
+        }
+        params.putInt("state", state)
+        
+        params.putDouble("bytesDownloaded", downloadState.bytesDownloaded.get().toDouble())
+        params.putDouble("bytesTotal", downloadState.bytesTotal.toDouble())
+        params.putString("destination", downloadState.destination)
+
+        foundTasks.pushMap(params)
+        processedIds.add(configId)
+      }
 
       logD(NAME, "getExistingDownloadTasks: found ${foundTasks.size()} tasks")
     }
diff --git a/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/ResumableDownloader.kt b/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/ResumableDownloader.kt
index ad93a37..c23da39 100644
--- a/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/ResumableDownloader.kt
+++ b/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/ResumableDownloader.kt
@@ -224,6 +224,12 @@ class ResumableDownloader {
 
   fun getBytesTotal(id: String): Long = activeDownloads[id]?.bytesTotal ?: -1
 
+  /**
+   * Get all active download states.
+   * Returns a map of download ID to DownloadState for all downloads currently being managed.
+   */
+  fun getAllActiveDownloads(): Map<String, DownloadState> = activeDownloads.toMap()
+
   private fun downloadWithResume(state: DownloadState, listener: DownloadListener, expectedSessionId: Long) {
     val result = executeDownload(state, listener, expectedSessionId)
 
diff --git a/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/uidt/UIDTJobState.kt b/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/uidt/UIDTJobState.kt
index 495e682..2f54140 100644
--- a/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/uidt/UIDTJobState.kt
+++ b/node_modules/@kesha-antonov/react-native-background-downloader/android/src/main/java/com/eko/uidt/UIDTJobState.kt
@@ -21,8 +21,8 @@ data class JobState(
  * Configuration for notification display.
  */
 data class NotificationConfig(
-    var groupingEnabled: Boolean = false,
-    var showNotificationsEnabled: Boolean = false,
+    var groupingEnabled: Boolean = true,
+    var showNotificationsEnabled: Boolean = true,
     var updateInterval: Long = 500L,
     val texts: MutableMap<String, String> = mutableMapOf(
         "downloadTitle" to "Download",
