import { ThemedStyles } from 'Theme/types';

export const styles = {
  empty: {
    height: '100%',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
} satisfies ThemedStyles;
