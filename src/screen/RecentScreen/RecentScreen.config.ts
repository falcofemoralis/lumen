export const NUMBER_OF_COLUMNS = 1;
export const THUMBNAILS_AMOUNT = 8;

// TV renders either a single-column list or, when the `recentTwoColumnsTV`
// setting is on, a two-column grid. Both keep the same number of rows on screen:
// the poster is as tall as its row, so fitting more rows in would shrink it.
export const NUMBER_OF_COLUMNS_TV = 1;
export const NUMBER_OF_COLUMNS_TV_TWO_COLUMNS = 2;
export const ITEMS_ON_SCREEN_TV = 3;
