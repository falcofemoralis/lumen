import CreateStyles from 'Util/CreateStyles';

export const styles = CreateStyles({
  container: {},
  voicesInput: {
    marginBottom: 10,
  },
  button: {
    marginEnd: 10,
    marginBottom: 10,
  },
  play: {
    flexDirection: 'row',
  },
});
