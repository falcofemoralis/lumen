import CreateStyles from 'Util/CreateStyles';

export const styles = CreateStyles({
  menuListWrapper: {
    height: 60,
    zIndex: 1,
  },
  menuListScroll: {},
  menuList: {
    gap: 15,
  },
  gridWrapper: {
    zIndex: 2,
  },
});
