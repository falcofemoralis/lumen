import CreateStyles from 'Util/CreateStyles';

export const styles = CreateStyles({
  menuListWrapper: {
    height: 50,
    paddingHorizontal: 16,
    zIndex: 1,
  },
  menuListScroll: {},
  menuList: {
    gap: 8,
  },
  gridWrapper: {
    zIndex: 2,
  },
});
