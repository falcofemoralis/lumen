import PlayerComponent from './Player.component';

export function PlayerContainer() {
  // is TV? return PlayerComponent.tv
  //  useKeepAwake();
  return <PlayerComponent />;
}

export default PlayerContainer;
