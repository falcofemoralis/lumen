import CreateStyles from 'Util/CreateStyles';

export const styles = CreateStyles({
  container: {
    backgroundColor: '#808080',
    height: '100%',
  },
  tab: {
    width: '100%',
    height: 20,
    backgroundColor: '#808080',
  },
  tabText: {
    color: '#fff',
  },
});
